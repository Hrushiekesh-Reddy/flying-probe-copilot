# Plan — 2026-06-13 — Phase 1a Synthetic HP3070 Log Generator

## Goal Contract

**OBJECTIVE:** Build `src/flying_probe_copilot/generator/` — a tested, configurable, CLI-driven Python module that emits HP3070 / Keysight i3070 ICT logs in the **real Keysight Log Record Format** (lexically conformant — `{@PREFIX|field|...}` syntax), plus CSV and JSON sidecars, from configurable board profiles + fault-injection parameters.

**SUCCESS-WHEN** (all must be true at end of Step 5):

1. Directory `src/flying_probe_copilot/generator/` exists with submodules: `models.py`, `profiles.py`, `faults.py`, `schedule.py`, `grammar.py`, `renderers/log.py`, `renderers/csv.py`, `renderers/json.py`, `cli.py`.
2. Directory `tests/test_generator/` exists with: `conftest.py`, `test_models.py`, `test_profiles.py`, `test_schedule.py`, `test_faults.py`, `test_grammar.py`, `test_renderers.py`, `test_cli.py`, `test_lexical_compliance.py`, `test_btest_status_derivation.py`, `test_seed_reproducibility.py`, `test_no_real_data_leak.py`.
3. CLI works: `python -m uv run generator --board-profile=small --count=10 --out=data/synthetic/` produces a run directory containing `config.yaml`, `manifest.json`, `logs/<serial>.log` × 10, `results.csv`, `results.json`.
4. `python -m uv run pytest -v` shows **all tests passing** with **≥90% coverage** on `src/flying_probe_copilot/generator/`.
5. Three board profiles (`small`, `medium`, `large`) exist and produce distinctly-sized runs matching spec lines 100-104.
6. Four fault profiles (`random`, `drift`, `cluster`, `process-change`) implemented and tested.
7. Generated `.log` files pass the lexical-compliance regex grammar from `grammar.py` (round-trip: generate → grammar.match() → all records valid).
8. `@BTEST` overall status is correctly derived from worst contained subtest status (covered by `test_btest_status_derivation.py`).
9. Performance check (informational only, not gating): 1000 small panels generate in ≤30 s.
10. Each new file has a corresponding test file that was committed/visible in the working tree BEFORE the implementation (verifiable from per-test-execution order in Step 5 log).

**OUT-OF-SCOPE** (executor must STOP and report if any are touched):

- ❌ `src/flying_probe_copilot/parser/` — Phase 1b
- ❌ `src/flying_probe_copilot/db/` and `schema.py` — Phase 1b
- ❌ `src/flying_probe_copilot/analytics/` — Phase 2
- ❌ `src/flying_probe_copilot/ui/` — Phase 2/3
- ❌ `src/flying_probe_copilot/rag/` — Phase 3
- ❌ Boundary-scan records (`@BS-CON`/`@BS-O`/`@BS-S`/`@NODE`) — deferred to Phase 5 per revised spec
- ❌ Probe records (`@PRB`/`@DPIN`) — deferred to Phase 5
- ❌ `@RPT` free-form report payload generation beyond a templated stub — deferred
- ❌ Real Keysight verbatim text in any source file or comment — guardrail
- ❌ Copying Virinco C# regex strings verbatim into Python source — derive equivalents from the Keysight format chapter (cited only)
- ❌ Any push to `origin/main` or `origin/dev` from agent — owner-only
- ❌ Mid-session commits — Step 8 only
- ❌ New top-level dependencies beyond what's already in `pyproject.toml` (pydantic explicit-declaration is the one exception, pre-approved)
- ❌ Modifying `.claude/settings.json` or `.env.example`
- ❌ Changing test runner config (pyproject `[tool.pytest.ini_options]`) — already configured

**CONSTRAINTS:**

- Branch: `feature/phase1a-generator` (currently active; verified `git branch --show-current`)
- TDD discipline: every step is **RED-test → GREEN-impl** in that order; executor MUST verify each test fails first
- Critical-file touches:
  - `pyproject.toml` — one addition (`pydantic>=2.0` explicit dependency). Owner-approved per Session Brief.
  - `.gitignore` — already touched this session (`.cache_research/` added). No further changes needed.
- No new dependencies beyond the pydantic explicit declaration
- Phase discipline: Phase 1a generator only
- All commits at Step 8 only

---

## What / Why / Where / When

| # | File | What | Why (deliverable / spec line) | When (after) | Test file |
|---|---|---|---|---|---|
| 1 | `src/flying_probe_copilot/__init__.py` | empty stub | Package needs to be importable | — | n/a |
| 2 | `src/flying_probe_copilot/generator/__init__.py` | empty stub | Subpackage import | 1 | n/a |
| 3 | `tests/__init__.py` | empty stub | Test package import | — | n/a |
| 4 | `tests/test_generator/__init__.py` | empty stub | Subpackage import | 3 | n/a |
| 5 | `tests/conftest.py` | shared pytest fixtures: `tmp_path` shortcuts, seeded `random.Random` factory, small/medium board profile dicts, fixed timestamp, sentinel sample BoardLog | testing.md lines 116-133; needed by every test below | 4 | n/a |
| 6 | `src/flying_probe_copilot/generator/models.py` | pydantic models + IntEnums per revised spec data model section | spec data model lines (revised 2026-06-13); ROADMAP line 33 | 5, after `test_models.py` RED | `test_models.py` |
| 7 | `src/flying_probe_copilot/generator/profiles.py` | `get_profile(name)` returning BoardProfile for small/medium/large | spec lines 100-104; ROADMAP line 35 | 6 | `test_profiles.py` |
| 8 | `src/flying_probe_copilot/generator/schedule.py` | timestamp clustering, shift assignment, operator/line stability | spec realism rules; ROADMAP line 34 | 6 | `test_schedule.py` |
| 9 | `src/flying_probe_copilot/generator/faults.py` | per-board failure picker (random/drift/cluster/process-change), per-record failure-code derivation, fault correlation within panel | spec fault profiles; spec failure distribution table; ROADMAP line 37 | 6, 7 | `test_faults.py` |
| 10 | `src/flying_probe_copilot/generator/grammar.py` | Python regex grammar derived from Keysight format chapter (NOT from Virinco regex source); exposes `validate(log_text)` and per-prefix patterns | spec test 6 (`test_lexical_compliance`); spec lexical micro-syntax section | 6 | `test_grammar.py` + used by `test_lexical_compliance.py` |
| 11 | `src/flying_probe_copilot/generator/renderers/__init__.py` | empty stub | Subpackage import | 6 | n/a |
| 12 | `src/flying_probe_copilot/generator/renderers/log.py` | render `BatchLog` to real-format text; CRLF default; scientific-notation floats; `@BTEST` status derivation helper | spec output format section; ROADMAP line 36 | 6, 10 | `test_renderers.py` + `test_lexical_compliance.py` + `test_btest_status_derivation.py` |
| 13 | `src/flying_probe_copilot/generator/renderers/csv_.py` | flatten all records to one row per test; columns: serial, batch_id, btest_status, block_designator, record_type, status, measured, lim_nominal, lim_high, lim_low, designator | ROADMAP line 36 | 6 | `test_renderers.py` |
| 14 | `src/flying_probe_copilot/generator/renderers/json_.py` | structured JSON: batch metadata + list of boards + per-board records (uses pydantic `.model_dump()`) | ROADMAP line 36 | 6 | `test_renderers.py` |
| 15 | `src/flying_probe_copilot/generator/cli.py` | argparse CLI; resolves config; orchestrates generator; writes config.yaml + manifest.json + log/csv/json outputs to run directory | spec CLI examples; ROADMAP line 38 | 7, 8, 9, 10, 12, 13, 14 | `test_cli.py` |
| 16 | `pyproject.toml` | add `pydantic>=2.0` to `[project] dependencies` (was transitively present; declare explicitly for clarity) | approval-gated, owner pre-approved in brief | 5 (before tests reference pydantic) | n/a (validated by `uv sync`) |

Note: filenames `csv_.py` and `json_.py` use trailing underscore to avoid clashing with stdlib `csv` and `json` at import time inside `renderers/`.

---

## Ordered execution steps (TDD)

> Each step block: **RED** (write failing test) → **GREEN** (write minimum implementation) → **pytest** (verify GREEN) → next step. Executor must NOT skip the RED-confirmation step.

### Phase 1 — Skeleton + models

**Step A1.** Create skeleton package directories: `src/flying_probe_copilot/{,generator/{,renderers/}}`, `tests/{,test_generator/}`. Add empty `__init__.py` in each. No tests yet (these are infrastructure).

**Step A2.** Edit `pyproject.toml`: add `"pydantic>=2.0"` to `[project] dependencies` list (keeping alphabetical order if any). Run `python -m uv sync` to validate.

**Step A3.** Write `tests/conftest.py` with fixtures: `seeded_random` (returns `random.Random(42)`), `small_profile_dict`, `medium_profile_dict`, `fixed_timestamp` (returns `datetime(2026, 4, 1, 8, 30, 0)`), `sample_panel` (returns a `PanelInstance` instance — will be RED until models exist).

**Step A4.** [RED] Write `tests/test_generator/test_models.py` with tests:
- `test_board_profile_validates_minimum_fields`
- `test_panel_instance_serial_format`
- `test_batch_record_field_count`
- `test_btest_record_status_uses_intenum`
- `test_analog_record_limits_tagged_union` (LIM2 vs LIM3 by record_type)
- `test_short_destination_pairs_well_formed`
- `test_btest_status_intenum_includes_all_documented_codes`

Run pytest, confirm ALL fail with ImportError.

**Step A5.** [GREEN] Implement `src/flying_probe_copilot/generator/models.py` per revised spec. Implement only enough to pass the above tests; no extras.

Run pytest, confirm `test_models.py` passes.

### Phase 2 — Board profiles

**Step B1.** [RED] Write `tests/test_generator/test_profiles.py`:
- `test_get_profile_small_returns_50_components_80_nets_120_tests`
- `test_get_profile_medium_returns_200_components_300_nets_450_tests`
- `test_get_profile_large_returns_800_components_1000_nets_1600_tests`
- `test_get_profile_unknown_name_raises_value_error`
- `test_profile_component_mix_sums_to_component_count`

Run pytest, confirm new tests fail.

**Step B2.** [GREEN] Implement `src/flying_probe_copilot/generator/profiles.py`. Run pytest, confirm green.

### Phase 3 — Schedule (timestamps + operators + lines + shifts)

**Step C1.** [RED] Write `tests/test_generator/test_schedule.py`:
- `test_timestamps_cluster_in_three_shifts` (asserts hour-of-day distribution clusters around 6/14/22, not uniform)
- `test_timestamps_weekday_heavy` (>=70% of timestamps fall on weekdays over a 4-week range)
- `test_operator_id_stable_within_shift` (same operator runs ≥50 consecutive panels)
- `test_line_id_assignment_uses_only_configured_lines`
- `test_panel_serial_format_matches_SYN_YYYYWww_NNNNN_pattern`
- `test_test_program_version_changes_infrequently` (≥100 panels between version bumps)

Run pytest, confirm fail.

**Step C2.** [GREEN] Implement `src/flying_probe_copilot/generator/schedule.py` with helpers: `generate_panel_schedule(start, end, count, seed, ops, lines)` → `list[PanelInstance]`. Run pytest, confirm green.

### Phase 4 — Grammar (lexical compliance regex)

**Step D1.** [RED] Write `tests/test_generator/test_grammar.py`:
- `test_grammar_accepts_minimal_batch_btest`
- `test_grammar_accepts_a_res_with_lim3`
- `test_grammar_accepts_a_cap_with_lim3`
- `test_grammar_accepts_a_dio_with_lim2`
- `test_grammar_accepts_d_t_with_substatus_bitmask`
- `test_grammar_accepts_ts_with_subrecords`
- `test_grammar_accepts_tjet_two_digit_status`
- `test_grammar_accepts_pf_with_pin_list`
- `test_grammar_rejects_unbalanced_braces`
- `test_grammar_rejects_unknown_prefix`
- `test_grammar_rejects_invalid_status_code_in_analog`
- `test_grammar_rejects_non_scientific_float`
- `test_grammar_rejects_invalid_timestamp_length`
- `test_grammar_rejects_a_res_with_lim2_instead_of_lim3` (LIM2/LIM3 record-type mismatch)
- `test_grammar_accepts_btest_12_field_and_13_field_forms`

Run pytest, confirm fail.

**Step D2.** [GREEN] Implement `src/flying_probe_copilot/generator/grammar.py`. Derive regex patterns from the Keysight format chapter description in `specs/synthetic-log-generator.md`. Cite "Keysight i3070 Log Record Format (chapter 8)" in module docstring; cite "Virinco WATS-Client-Converter (LGPL-3.0)" only as the cross-validation reference, NOT the source of regex strings. Expose: `Grammar.match_batch(text) -> match`, `Grammar.validate(log_text) -> list[GrammarError]`. Run pytest, confirm green.

### Phase 5 — Faults

**Step E1.** [RED] Write `tests/test_generator/test_faults.py`:
- `test_fault_profile_random_rate_within_tolerance_over_500_panels` (±20% of `fault_rate` target)
- `test_fault_profile_drift_increases_monotonically` (split date range into 4 quartiles, assert rate(q4) > rate(q1))
- `test_fault_profile_cluster_concentrates_in_narrow_windows` (≥80% of failures fall in <20% of timestamps)
- `test_fault_profile_process_change_step_change` (rate before/after the change-point differs by ≥2×)
- `test_failure_mode_distribution_matches_spec_within_tolerance` (40/25/15/10/7/3 ±5pp)
- `test_btest_status_for_shorts_failure_is_4`
- `test_btest_status_for_analog_failure_is_6`
- `test_btest_status_for_digital_failure_is_8`
- `test_fault_correlation_within_panel` (when one R fails, neighboring-net Rs have elevated fail prob)

Run pytest, confirm fail.

**Step E2.** [GREEN] Implement `src/flying_probe_copilot/generator/faults.py`. Run pytest, confirm green.

### Phase 6 — Renderers

**Step F1.** [RED] Write `tests/test_generator/test_renderers.py` (one class per renderer):
- `TestLogRenderer::test_emits_balanced_braces`
- `TestLogRenderer::test_emits_scientific_notation_floats_with_six_mantissa_digits`
- `TestLogRenderer::test_emits_timestamp_in_yymmddhhmmss_format`
- `TestLogRenderer::test_emits_lim3_after_a_res`
- `TestLogRenderer::test_emits_lim2_after_a_dio`
- `TestLogRenderer::test_emits_crlf_line_endings_by_default`
- `TestLogRenderer::test_emits_utf8_lf_when_encoding_flag_set`
- `TestLogRenderer::test_btest_status_derived_from_worst_subtest`
- `TestCsvRenderer::test_one_row_per_test_record`
- `TestCsvRenderer::test_csv_parseable_by_stdlib`
- `TestCsvRenderer::test_columns_include_serial_block_record_type_status_measured`
- `TestJsonRenderer::test_json_parseable_by_stdlib`
- `TestJsonRenderer::test_json_round_trips_through_pydantic`

Run pytest, confirm fail.

**Step F2.** [GREEN] Implement `renderers/log.py`, `renderers/csv_.py`, `renderers/json_.py`. The log renderer's `@BTEST` status derivation helper is reused by faults — but the actual derivation function lives in `models.py` to keep the renderer pure. Run pytest, confirm green.

### Phase 7 — Lexical compliance integration test

**Step G1.** [RED] Write `tests/test_generator/test_lexical_compliance.py`:
- `test_small_run_output_passes_grammar` (generate 10 small-profile panels, run grammar.validate over each `.log` file, assert zero errors)
- `test_medium_run_output_passes_grammar` (generate 5 medium-profile panels, assert grammar accepts)
- `test_drift_profile_output_passes_grammar`

Write `tests/test_generator/test_btest_status_derivation.py`:
- `test_pass_only_records_produce_btest_status_0`
- `test_shorts_failure_produces_btest_status_4`
- `test_analog_failure_only_produces_btest_status_6`
- `test_digital_and_analog_failures_use_priority_table`

Run pytest, confirm fail.

**Step G2.** [GREEN] Wire up generator orchestration to make these pass. Likely no new code — this exercises the composition of phases 2-6. Run pytest, confirm green.

### Phase 8 — CLI + end-to-end

**Step H1.** [RED] Write `tests/test_generator/test_cli.py`:
- `test_cli_help_lists_all_documented_flags`
- `test_cli_run_writes_run_directory_with_expected_files` (config.yaml, manifest.json, logs/, results.csv, results.json)
- `test_cli_unknown_board_profile_exits_non_zero`
- `test_cli_resolved_config_yaml_round_trips_through_yaml_loader`
- `test_cli_manifest_json_contains_panel_count_fault_rate_seed`

Write `tests/test_generator/test_seed_reproducibility.py`:
- `test_same_seed_produces_byte_identical_log_files`
- `test_same_seed_produces_byte_identical_csv`
- `test_same_seed_produces_byte_identical_json`

Write `tests/test_generator/test_no_real_data_leak.py`:
- `test_generated_logs_contain_no_sentinel_strings` (sentinels: "customer", "confidential", "proprietary")

Run pytest, confirm fail.

**Step H2.** [GREEN] Implement `src/flying_probe_copilot/generator/cli.py`. Use `argparse`; `pyyaml` (already in transitive deps via chromadb / sentence-transformers? — verify in `uv sync` output; if NOT, add to dependencies and re-approve with owner before adding). Run pytest, confirm green.

### Phase 9 — Coverage gate

**Step I1.** Run `python -m uv run pytest -v --cov=src/flying_probe_copilot/generator --cov-report=term-missing`. Confirm ≥90% line coverage on the generator subpackage. If <90%, add tests for uncovered branches (no code changes).

### Phase 10 — Performance smoke

**Step J1.** Run a 1000-small-panel generation; record elapsed time. Informational only — if >30 s, log in BUG_LOG as a P2 perf item; do NOT optimize this session.

---

## Guardrails

| Constraint | How it's enforced this session |
|---|---|
| Branch = `feature/phase1a-generator` | Verified via `git branch --show-current` before Step 5 begins |
| Critical files | Only `pyproject.toml` (single add of `pydantic>=2.0`; pre-approved). No other approval-gated files touched. |
| New dependencies | pydantic explicit declaration (pre-approved). If `pyyaml` needs to be added (Step H2), executor STOPS and asks owner. |
| Phase discipline | Phase 1a only — generator module + tests. No parser/db/analytics/UI/RAG code. |
| No mid-session commits | Confirmed — all commits at Step 8 |
| No `--no-verify`, no force-push, no `git reset --hard` | block_dangerous_git hook blocks; executor never invokes these |
| Out-of-scope bugs | Logged in `docs/logs/BUG_LOG.md` immediately, surfaced via spawn_task at Step 8; never fixed inline |
| TDD order | Every step has RED-confirmation step; executor must verify failure before writing implementation |
| Virinco IP boundary | Regex grammar derived from Keysight format chapter (in spec) only; Virinco repo cited as cross-validation reference, not as regex source; no verbatim C# code in Python files |
| Real-data guardrail | `test_no_real_data_leak` is a positive guardrail; no sentinel strings in generator output |
| Cache cleanup | Step 8 deletes `.cache_research/` directory (already gitignored as of this session) |

---

## Cross-cutting notes for executor

1. **`@BTEST` status derivation** lives in `models.py` (not in renderer) — pure function `derive_btest_status(blocks: list[TestBlock]) -> BTESTStatus` so the renderer remains a serializer.
2. **Float formatting** — use `"{:+.6E}".format(x)` for the analog `measured` field. Verify the precise format Keysight uses against the spec; some specs use unsigned mantissa.
3. **Empty-field handling** — pydantic optional fields render as empty (adjacent `||`), not omitted.
4. **Timestamp format** — Python `datetime.strftime("%y%m%d%H%M%S")` gives `YYMMDDHHMMSS`.
5. **BRRCC pin format** — for v1 the generator can use deterministic mapping `B=board_number(1)`, `RR=00..99`, `CC=00..99`.
6. **Reproducibility** — every random source must be a `random.Random(seed)` instance, never the global `random`. Tests assert byte-identical output for fixed seed.
7. **Encoding** — write `.log` with `open(path, "w", encoding="cp1252", newline="\r\n")` by default; `"utf-8"` + `"\n"` for `--encoding=utf8`.

---

## Step 4 (Verify Plan) input

Pass this plan, plus the Step 2 web-research report (which I have in conversation memory; executor can re-read it from the AGENT subagent transcript if needed), plus the revised spec, to the adversarial Plan Reviewer subagent.

Specifically ask the reviewer to challenge:

1. Is the grammar derivation methodology airtight against the Virinco LGPL boundary?
2. Are any test assertions over-tight (e.g. ±20% tolerance over 500 panels — is N=500 statistically sufficient)?
3. Did I miss any spec deliverable (ROADMAP lines 32-43)?
4. Is `pyyaml` actually a transitive dependency? If not, do I have a clean alternative (stdlib `json` for both — drop `config.yaml` in favor of `config.json`)?
5. Have I correctly mapped each pydantic model to its real-format record without scope creep into Phase 1b?
6. Is the BTEST status derivation priority order correct (per the spec table)?

## Status

Plan v1 written. Red-teamed (Step 4). Verdict: BLOCK with 3 BLOCKERs + 6 WARNINGs/MINORs. **All concerns now resolved — see Revision 1 below.** Awaiting owner go-ahead before Step 5 (Execute).

---

## Revision 1 — 2026-06-13 — Address Step 4 red-team concerns

The Plan Reviewer subagent (Step 4) returned 3 BLOCKERs and 6 WARNINGs/MINORs. All ten resolutions, owner-approved or parent-decided:

### BLOCKER #1 — `pyyaml` dependency [RESOLVED via owner sign-off]
Owner approved adding `pyyaml>=6.0` to `pyproject.toml` `[project] dependencies`. This makes pyyaml a first-class dependency, not a fragile transitive one. The YAML config decision in the brief stands.

**Plan change:** Step A2 (pyproject edit) becomes a 2-line addition, not 1:
- Insert `"pydantic>=2.0",` between `plotly>=5.24` and `python-dotenv>=1.0` (alphabetical-ish ordering not enforced; explicit position).
- Insert `"pyyaml>=6.0",` immediately after `pydantic>=2.0`.
Both are pre-approved by owner this session.

### BLOCKER #2 — `conftest.py` fixture would break collection [RESOLVED]
The `sample_panel` fixture cannot live in `conftest.py` at Step A3 because importing `PanelInstance` at module top would break pytest collection of every test before Step A5 creates the models module.

**Plan change:**
- Step A3 `conftest.py` MAY ONLY define fixtures whose body imports nothing from `flying_probe_copilot.*` at module top level. Permitted at A3: `seeded_random`, `small_profile_dict`, `medium_profile_dict`, `fixed_timestamp`.
- `sample_panel` (and any other model-dependent fixture) is created in `tests/test_generator/conftest.py` as part of Step A5 (after `models.py` lands), or inline in `test_models.py` to start. The repo-root `tests/conftest.py` stays model-free.

### BLOCKER #3 — `@BTEST` status priority undefined [RESOLVED via owner sign-off]
Owner picked **categorical precedence rule**: `SHORTS (4) → ANALOG (6) → DIGITAL (8) → PIN (2) → TJET (14) → POLARITY (15) → CCHK (16) → FUNCTIONAL (9) → POWER (7) → UNCATEGORIZED (1)`. If no failure, status = `PASS (0)`. Codes 11–13 (handler/barcode/X'd-out) and 80–82 (runtime/abort) are environmental and never derived from subtest status — they're set externally.

**Plan change:**
- Add `models.derive_btest_status(blocks: list[TestBlock]) -> BTESTStatus` as a pure function. Algorithm: scan blocks in the priority order above; first match wins. Implementation is a small ordered-list lookup, ~12 lines.
- Step A4 adds `test_derive_btest_status_priority_categorical` to `test_models.py`:
  - `test_pass_only_returns_PASS`
  - `test_any_shorts_failure_returns_FAIL_SHORTS_even_with_other_failures`
  - `test_analog_failure_returns_FAIL_ANALOG_when_no_shorts`
  - `test_digital_failure_when_only_digital_block_failed`
  - `test_pin_failure_when_only_pf_record_failed`
  - `test_environmental_codes_not_derived`
- `test_btest_status_derivation.py` (Step G1) shrinks to integration cases using the helper directly.

### WARNING #4 — CRLF test must read binary [RESOLVED]
**Plan change:** Step F1 explicitly requires:
```python
def test_emits_crlf_line_endings_by_default(tmp_path, sample_batch_log):
    out = tmp_path / "x.log"
    render_log(sample_batch_log, out)
    raw = out.read_bytes()
    assert b"\r\n" in raw
    # No bare LF that isn't preceded by CR
    assert b"\n" not in raw.replace(b"\r\n", b"")
```
Same pattern for `test_emits_utf8_lf_when_encoding_flag_set` (binary read, assert `b"\n"` present and `b"\r\n"` absent).

### WARNING #5 — Failure-mode distribution tolerance flaky [RESOLVED]
**Plan change:** Step E1's `test_failure_mode_distribution_matches_spec_within_tolerance` becomes deterministic-exact:
- Seed `random.Random(42)`, generate 10,000 panels with `fault_rate=0.10` (yields ~1000 failures).
- Assert empirical share for each failure mode is within ±2 percentage points of the spec target (40/25/15/10/7/3).
- Additionally, save a "golden" failure-count snapshot for seed 42 (e.g. expected_counts={"ANALOG_OOL": 401, "SHORTS": 248, ...}) and assert exact match. This makes the test stable across runs while still validating distribution shape.
- Sample size and ±2pp picked so that even the 3% category (~30 failures) has a sampling-error band of ±sqrt(30)/1000 ≈ ±0.55pp.

### WARNING #6 — Cache cleanup [RESOLVED]
**Done.** `.cache_research/` was deleted immediately after this revision was written; verified via `ls .cache_research/` returning "No such file or directory".

### MINOR #7 — pyproject insertion location ambiguity [RESOLVED]
**Plan change:** Step A2 (above) now specifies exact insertion location: between `plotly>=5.24` and `python-dotenv>=1.0`.

### WARNING #8 — Fault correlation needs net-graph [RESOLVED]
**Plan change:** `faults.py` synthesizes a sparse "net adjacency" structure at panel-instantiation time, using a refdes-numerical clustering heuristic (documented in module docstring):
- Within a panel, components with adjacent refdes numerals (R12 ↔ R13 ↔ R14) are treated as "neighbors" with probability `0.7`. Components with `|i - j| ≤ 3` get `0.3`. All others `0`.
- When a component fails, neighbors get a `1.5×` failure-rate multiplier for the same panel.
- Defensible heuristic: real PCBAs do exhibit reference-designator-locality (adjacent refdes often share routing concerns). It's a synthesis convenience, not a claim of physical accuracy, and is documented as such.

**New test** in Step E1: `test_correlation_uses_refdes_numerical_neighbor_heuristic` — over 1000 seeded panels where R12 is forced to fail, assert R13's empirical fail rate > 1.4× the baseline.

### MINOR #9 — Tagged-union validator for `AnalogRecord.limits` [RESOLVED]
**Plan change:** `models.py` `AnalogRecord` gets a `@model_validator(mode='after')`:

```python
LIM2_TYPES = {AnalogType.DIO, AnalogType.FUS, AnalogType.JUM, AnalogType.MEA,
              AnalogType.NFE, AnalogType.NPN, AnalogType.PFE, AnalogType.PNP, AnalogType.SWI}
LIM3_TYPES = {AnalogType.CAP, AnalogType.IND, AnalogType.POT, AnalogType.RES, AnalogType.ZEN}

@model_validator(mode='after')
def _limits_match_record_type(self):
    if self.record_type in LIM2_TYPES and not isinstance(self.limits, Limits2):
        raise ValueError(f"{self.record_type} requires Limits2, got Limits3")
    if self.record_type in LIM3_TYPES and not isinstance(self.limits, Limits3):
        raise ValueError(f"{self.record_type} requires Limits3, got Limits2")
    return self
```

**New test** in Step A4: `test_analog_record_rejects_wrong_limit_type_for_record_type`. The grammar-level test (Step D1 `test_grammar_rejects_a_res_with_lim2_instead_of_lim3`) becomes belt-and-suspenders.

### MINOR #10 — Float formatting locked [RESOLVED]
**Plan change:** All analog/shorts/digital measured values render via `"{:+.6E}".format(value)` — explicit `+` for positive mantissas, six-digit mantissa, capital `E`, signed exponent. Documented in `renderers/log.py` module docstring. No further hedge in the plan.

---

## Revision 1 status

All BLOCKERs and WARNINGs from Step 4 resolved. Plan is now executable. Awaiting owner go-ahead for Step 5 (Execute).
