# Session Brief — 2026-06-13

## What the owner wants

> "Begin Phase 1a — Synthetic HP3070 Log Generator. Run /session-workflow and follow the 10-step loop. The goal is to build `src/flying_probe_copilot/generator/` — a Python module that generates realistic HP3070-style flying-probe test logs from configurable parameters. Full spec is in `specs/synthetic-log-generator.md`. Spawn an Explore subagent to research the HP3070 / Keysight i3070 log format (public Keysight docs, open-source ICT tools, manufacturing forums — owner does NOT have the manual locally), what fields a real log contains, and what existing open-source HP3070 parsers exist."

The owner also requested an honest verification of the Phase 0 setup and disadvantages that will affect the project. That assessment was delivered before invoking this skill; the results have been logged informally as **out-of-scope follow-ups** (two `spawn_task` chips active, full setup-audit results to be incorporated in Step 8 documentation).

## Goal statement (one sentence)

Build `src/flying_probe_copilot/generator/` — a tested, configurable, CLI-driven Python module that produces realistic HP3070-style flying-probe ICT test logs in `.log` (text), `.csv`, and `.json` formats from synthetic board profiles + fault-injection parameters.

## Success looks like

The following must be observably true at end-of-session:

1. **Module created at `src/flying_probe_copilot/generator/`** with submodules: `models.py`, `profiles.py`, `faults.py`, `schedule.py`, `renderers/`, `cli.py`.
2. **CLI entry point works** — `uv run flying-probe-gen --board-profile=small --count=10 --out=data/synthetic/` produces an output run directory with `config.yaml`, `manifest.json`, `logs/*.log`, `results.csv`, `results.json`.
3. **3 board profiles exist** — `small`, `medium`, `large` — producing distinctly-sized panel runs.
4. **4 fault profiles implemented** — `random`, `drift`, `cluster`, `process-change`.
5. **All tests pass** under `uv run pytest`:
   - `test_schema_validity` — every generated panel matches the data model
   - `test_seed_reproducibility` — same seed → identical bytes-on-disk
   - `test_fault_rate_within_tolerance` — empirical fault rate within ±20% of target over 500 panels
   - `test_no_real_data_leak` — sentinel string scan passes
   - `test_output_files_present` — log, csv, json, manifest, config all exist after a run
6. **Performance check** — 1,000 panels generate in <30 s (informational, not gating).
7. **Test coverage ≥90% on the generator module.**
8. **TDD discipline observable** — every test file precedes its implementation file in the commit history of `feature/phase1a-generator`.

## Out of scope (explicit)

The following are NOT done this session and any subagent that touches them must STOP and report:

- ❌ **Parser module** (`src/flying_probe_copilot/parser/`) — Phase 1b
- ❌ **DuckDB schema** — Phase 1b
- ❌ **Analytics** (yield, Pareto, SPC, anomaly) — Phase 2
- ❌ **Streamlit dashboard or any UI** — Phase 2/3
- ❌ **RAG / ChromaDB / embeddings / Gemini API integration** — Phase 3
- ❌ **Real Keysight log content verbatim** — guardrail (owner does not have manuals; only public-source-derived format is acceptable)
- ❌ **Real customer / proprietary data** — guardrail
- ❌ **`pyproject.toml` modifications** — approval-gated, unless owner explicitly approves removing the `parser` script entry (open question, see below)
- ❌ **`.claude/settings.json` changes** — approval-gated
- ❌ **GitHub Actions / CI** — Phase 4
- ❌ **Boundary-scan vector test detail** — spec defers to "probably Phase 5"
- ❌ **Takaya or non-HP3070 log formats** — locked decision
- ❌ **Real-time file watcher / ingest** — Phase 2 decision point
- ❌ **Open-sourcing the generator as its own package** — Phase 4 decision point

## Phase / milestone

**Phase 1a** in `docs/ROADMAP.md`. Maps to deliverables lines 32–43 inclusive.

## Branch

`feature/phase1a-generator` — must be created from a clean `main` before any file under `src/` or `tests/` is written.

Verify before Step 5 (Execute):
```powershell
git status              # clean
git branch --show-current  # confirm not 'main' or 'dev' once branch created
```

## Hard constraints (carried into every subagent charter)

1. **TDD non-negotiable** — failing test BEFORE its implementation, every step, no exceptions (`.claude/rules/testing.md`).
2. **No real customer log data** — only synthetic generator output enters the repo (`docs/GUARDRAILS.md`, `CLAUDE.md`).
3. **No proprietary Keysight text verbatim** — format reconstructed from public sources only (CLAUDE.md hard guardrail #3).
4. **No commits to `main` or `dev` directly** — `feature/phase1a-generator` only (`.claude/rules/agent-conduct.md`).
5. **No new dependencies** — entire stack already in `pyproject.toml`. Any `uv add` requires owner approval.
6. **No mid-session commits** — commit only at Step 8 documentation.
7. **No `--no-verify`, no force-push, no `git reset --hard`** — block_dangerous_git hook enforces this.
8. **One phase per session** — do not write parser, schema, or analytics code (Phase 1b/2).
9. **Out-of-scope bugs go to `docs/logs/BUG_LOG.md` immediately** — never fixed silently.
10. **Critical files** — `pyproject.toml`, `db/schema.py` (does not exist yet), `migrations/*` (does not exist yet), `.claude/settings.json`, `.env.example` — require owner sign-off before any edit.

## Pre-flight verification (carried from setup audit)

Before Step 5 can begin, the following environment items MUST be confirmed by the owner:
- [ ] `uv` is installed and on PATH (`uv --version` succeeds in PowerShell)
- [ ] `uv sync` has been run successfully and `.venv/` exists
- [ ] Owner approves removing or deferring the `parser` entry point in `pyproject.toml` (it points to a Phase 1b module that does not exist; running `uv run parser` would crash)

If `uv` is still not installed at Step 5 time: pause and report. Do not improvise an alternative.

## Open questions — RESOLVED 2026-06-13

1. **Data model:** **pydantic v2** (owner-approved). Free runtime validation for `test_schema_validity`. Pydantic is already pulled transitively by chromadb — no new top-level dep. Add explicit `pydantic>=2.0` declaration to `pyproject.toml` in Step 5 (approval-gated; owner approval implicit via choice of pydantic).
2. **CLI config UX:** **CLI flags only for v1, with resolved config written to `config.yaml` in the output run directory** (owner-approved). No `--config` input file in v1. Spec examples (lines 164–187) work as-is.
3. **CLI tests location:** `tests/test_generator/test_cli.py` (default).
4. **`parser` entry point:** **Removed** (owner-approved). Comment placeholder left in `pyproject.toml` referencing `docs/ROADMAP.md` for re-add timing. Applied during housekeeping (uncommitted, will be part of the Step 8 commit).
5. **Saved run config format:** **YAML** (`data/synthetic/<run_id>/config.yaml`) per spec.

## Housekeeping completed (Phase 0 cleanup, pre-Step 2) — 2026-06-13

The repo arrived with 6 local branches + 1 stash overlapping in messy ways. Resolved before any Phase 1a work:

- Dropped `stash@{0}` — content superseded by `feature/gitignore-data-synthetic-v2` (confirmed via diff-of-diffs).
- Deleted `feature/gitignore-synthetic-data` (was an empty branch = main).
- Deleted `feature/pyproject-init` (already merged into main via `d822ed3`).
- Merged → main (per owner-approved fast path): `fix/commit-uv-lock`, `feature/gitignore-data-synthetic-v2`, `feature/pyproject-dependency-groups`.
- Fast-forwarded `dev` from `main`.
- Deleted the 3 just-merged feature branches with safe `-d` (git verified each was fully merged).
- Created `feature/phase1a-generator` from cleaned main.
- Ran `python -m uv sync` — 158 packages resolved, `.venv/Scripts/generator.exe` present, `parser.exe` removed after pyproject edit, `python -m uv run pytest --version` → pytest 9.1.0.
- Decision log entry to be added in Step 8 documenting the fast-path merge exception (feature→main direct, not feature→dev→main) as a one-time owner-approved choice.

**Phase C still pending:** owner must run the push commands manually (the `block_dangerous_git` hook correctly refuses any push to `main`/`dev` from agents). Commands to run from PowerShell in `E:\flying-probe-copilot`:

```powershell
git push origin main
git push origin dev
git push origin --delete feature/pyproject-init   # cleans up stale remote-only branch
```

Phase C does not block Step 2 (Explore). Phase 1a development can proceed in parallel with the owner-initiated push.

## uv install note

`uv` is installed via pip (v0.11.21) but the executable lives at `...Python311\Scripts\uv.exe` which is not on PATH. Workaround: this session uses `python -m uv ...` everywhere. For long-term ergonomics, owner should install `uv` via the official PowerShell installer:

```powershell
irm https://astral.sh/uv/install.ps1 | iex
```

That puts `uv.exe` on PATH globally. Not blocking; log as a deferred BUG_LOG entry in Step 8 if owner agrees.

## Reference materials

- `specs/synthetic-log-generator.md` — full spec, owner-authored
- `docs/ROADMAP.md` lines 27–45 — Phase 1a deliverables and exit criteria
- `CLAUDE.md` — project identity, hard guardrails
- `.claude/rules/agent-conduct.md` — branching, file approvals, commit policy
- `.claude/rules/testing.md` — TDD rules, coverage targets, conftest template
- `.claude/rules/session-workflow.md` — the 10-step loop summary

## Status

Step 1 — **DONE**. Proceed to Step 2 (Explore) once owner reviews this brief and answers the 5 open questions, or signals to proceed with reasonable defaults.
