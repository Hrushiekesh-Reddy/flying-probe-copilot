## Session Brief — 2026-06-14

### What the owner wants
> "i am ready to phase1b plan and i think its large task"

Begin Phase 1b (Parser & DuckDB Schema) using the full 10-step session-workflow loop at **Large tier**. This session's deliverable is the parser module plus the DuckDB schema and ingest CLI — the things Phase 1b's exit criteria depend on. Phase 2 analytics, Streamlit, and the notebook stay out.

### Goal statement (one sentence)
Stand up `src/flying_probe_copilot/parser/`, a DuckDB schema under `src/flying_probe_copilot/db/`, and an ingest CLI such that running the generator → running the parser → querying the resulting DuckDB file returns the correct "yield by board over the last week" answer for synthetic input, with round-trip integrity ≥99% on generator output.

### Success looks like
Measurable, observable outcomes that must all be true at end of session:

1. **Module exists:** `src/flying_probe_copilot/parser/` package with at least a `cli.py` entry point and a `log_parser.py` (lexer/record parser over the real Keysight Log Record Format).
2. **Schema exists:** `src/flying_probe_copilot/db/schema.py` defines DuckDB DDL for dimension tables (`boards`, `panels`, `operators`, `components`, `tests`) + fact tables (`test_runs`, `measurements`, `failures`). One canonical `CREATE TABLE` per entity, idempotent (`CREATE TABLE IF NOT EXISTS`) — **this file is approval-gated; owner sign-off required before Step 5 touches it.**
3. **CLI works:** `uv run parser --input=data/synthetic/<run_id>/ --db=data/db/flying-probe.duckdb` ingests every generator-produced `.log` (small / medium / large profiles) into the DuckDB file without crashing.
4. **Round-trip test passes:** `tests/test_parser/test_roundtrip.py::test_generator_to_parser_to_db_preserves_counts` — generate N panels with a fixed seed → parse → query DB → row counts and key sums (e.g. total measurements, total failures, btest_status distribution) match the generator's in-memory `BatchLog` within tolerance ≥99%.
5. **Yield query works:** `tests/test_analytics/...` is OUT OF SCOPE, but the query the exit criterion names lives in a parser-side test: `tests/test_parser/test_yield_query.py::test_yield_by_board_over_last_week_returns_expected` — ingest a fixture spanning ≥2 weeks across ≥2 boards → run the SQL → assert per-board yield (PASS / TOTAL) over the last 7 days matches the deterministic ground truth from the fixture.
6. **Malformed-line policy:** `tests/test_parser/test_log_parser.py::test_malformed_line_skipped_and_logged_not_crash` — a doctored log with a corrupt record line is parsed without exception; the bad line is captured (returned in a `parse_report` or logged) and the rest of the file ingests cleanly.
7. **Test suite:** ≥95% line coverage on `src/flying_probe_copilot/parser/` (per `.claude/rules/testing.md` Phase 1b target). Existing 98 generator tests still pass.
8. **CLI registered:** `pyproject.toml` re-adds `parser = "flying_probe_copilot.parser.cli:main"` to `[project.scripts]` (this is pre-approved per the 2026-06-13 handoff, line 107).
9. **Docs ticked:** ROADMAP Phase 1b deliverable boxes ticked in `docs/ROADMAP.md`; SESSION_LOG entry written; CLAUDE.md session-log line + phase-table line updated.

### Out of scope (explicit — do NOT do this session)
- **Analytics functions** (yield-over-time helper, Pareto, SPC, anomaly detection). Phase 2.
- **Streamlit UI** of any kind. Phase 2.
- **`notebooks/01-queries.ipynb`** — listed under Phase 1b deliverables on the roadmap but explicitly deferred this session as a documentation-only follow-up (matches how Phase 1a deferred its module README). Surface as a separate task if owner wants it next.
- **ChromaDB / sentence-transformers / BM25 / Gemini.** Phase 3.
- **CSV / JSON ingest paths.** Parser targets the `.log` file (real Keysight Log Record Format) only — the CSV/JSON renderers are convenience exports for humans, not the canonical ingest format. CSV/JSON ingest can be a Phase 4 polish item if useful.
- **Refactoring the generator.** Bug fixes only if a Phase 1b test surfaces a real bug; otherwise log to BUG_LOG and surface via spawn_task per the standing rule.
- **Multi-format support** (Takaya, etc.). Roadmap decision point — defer.
- **Real customer data ingest.** CLAUDE.md hard guardrail #1.
- **Generator module README** (the one Phase 1a deferred). Still deferred; not this session.
- **`uv add` / new dependencies.** DuckDB is already in `pyproject.toml`. No new packages this session.

### Phase / milestone
ROADMAP.md Phase 1b — Parser & DuckDB Schema (lines 51-65). Exit criterion: "Yield by board over last week query returns correct result. Round-trip test passes for ≥99% of generator output." Both criteria are folded into Success-3/4/5 above.

### Branch
`feature/phase1b-parser` — to be created from `dev` at the start of Step 5 (per `.claude/rules/agent-conduct.md`, never commit directly to `dev` or `main`). Current branch is `dev`, working tree clean (verified at brief time).

### Tier
**Large** — full 10-step loop. Justified by: new top-level module + new approval-gated schema file + new CLI + cross-file integration test (generator → parser → DB) + multi-file edit (parser package, db package, pyproject.toml, tests).

### Critical / approval-gated files this session may touch
Per `.claude/rules/agent-conduct.md`:

| File | Status | Action |
|------|--------|--------|
| `src/flying_probe_copilot/db/schema.py` | NEW (approval-gated category) | Owner sign-off required before Step 5 creates it. Brief assumes go-ahead. |
| `pyproject.toml` | approval-gated | Re-add `parser` script entry only (pre-approved 2026-06-13 handoff line 107). No new dependencies. |
| `migrations/` | approval-gated | Not touched this session — Phase 1b uses `CREATE TABLE IF NOT EXISTS` against an ephemeral local DB, no migration framework yet. Migration scaffolding deferred to Phase 2 or later. |
| `.claude/settings.json` | approval-gated | Not touched. |
| `.env.example` | approval-gated | Not touched. |

### Guardrails reaffirmed for this session
- TDD: Red → Green → Refactor per test, no implementation without a failing test first (`.claude/rules/testing.md`).
- One coherent commit at Step 8 (not mid-session).
- Out-of-scope bugs → `BUG_LOG.md` + spawn_task chip at Step 8, never silently fixed.
- No push without explicit owner request.
- Web/fetch tools: not expected this session. Format is already distilled in `specs/synthetic-log-generator.md` (per 2026-06-13 handoff line 115). If any subagent does pull external material, External-Research charter applies — scratch in `%TEMP%\agent-research\<session>\`, citations only, `Cleanup:` line mandatory.

### Open questions for owner before Step 5
1. **Sign-off on `src/flying_probe_copilot/db/schema.py` creation** (approval-gated path). Brief proposes one canonical DDL file under a new `db/` subpackage. Alternative: nest under `parser/schema.py`. Recommendation: `db/` — schema is consumed by parser AND will be consumed by analytics (Phase 2) and RAG (Phase 3); keeping it under `parser/` would force later phases to import from a sibling module they don't otherwise depend on.
2. **DuckDB file location:** plan proposes `data/db/flying-probe.duckdb` (matches the CLI example in ROADMAP line 59). The `data/db/` directory will need a `.gitignore` rule (the `.duckdb` file itself is build output, not source).
3. **`notebooks/01-queries.ipynb`** — ROADMAP lists it as a Phase 1b deliverable but brief defers it. Confirm OK to defer to a separate doc-only session (matches the Phase 1a generator-README pattern).

These three are flagged here so they are resolved BEFORE Step 3 plan-writing, not in the middle of execution.

### Resolutions (owner, 2026-06-14)
1. **Schema location:** `src/flying_probe_copilot/db/schema.py` — approved (new `db/` subpackage, kept neutral for Phase 2/3 consumers).
2. **`notebooks/01-queries.ipynb`:** deferred to a separate doc-only session. Parent must surface a `spawn_task` chip at Step 8 so it isn't lost.
3. **DB file location:** `data/db/flying-probe.duckdb` approved as default; `.gitignore` rule to be added narrowing to `data/db/*.duckdb` + `data/db/*.duckdb.wal`, with `data/db/.gitkeep` committed so the directory exists on fresh clone.

---

**Next:** Step 2 (Explore subagent).
