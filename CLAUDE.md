# CLAUDE.md — Memory Bridge for Flying-Probe Co-Pilot

> Read this file at the start of every Claude Code session. It captures persistent project context so sessions stay focused and the context window stays clean.

## Project identity

- **Name:** Flying-Probe / ICT Test-Log Intelligence Co-Pilot
- **One-liner:** Parses PCBA flying-probe / ICT test logs into a SQL database, runs yield + anomaly analytics, and answers natural-language root-cause questions via hybrid RAG.
- **Owner:** Hrushiekesh Reddy Kanjula (Manufacturing Engineer, ~4 yrs PCBA, Dallas TX)
- **Why it exists:** Flagship AI portfolio project to land a Manufacturing/Process Engineer with AI role.
- **Status:** Phase 4 — Polish 🚧 **slice 3 IN PR** (GitHub Actions CI workflows + ruff config, 2026-06-21). Two NEW workflow files under `.github/workflows/` (greenfield CI — `.github/` didn't exist before): `ci.yml` runs `ruff check . --output-format=github` + `ruff format --check .` (lint job) and `uv run pytest -q --cov=src/flying_probe_copilot --cov-report=term` (tests job) in parallel on every PR to `dev`/`main`, ubuntu-latest, Python 3.11, `astral-sh/setup-uv@v8`, `uv sync --frozen --all-groups`, top-level `concurrency.cancel-in-progress: true`, `permissions: contents: read`, `paths-ignore: docs/** + **/*.md + notebooks/** + .claude/**`. `screenshots.yml` triggers on PRs touching `src/flying_probe_copilot/ui/**`, `src/flying_probe_copilot/analytics/**`, `docs/knowledge-base/**`, `scripts/capture_screenshots.py`, `scripts/_capture_app.py`; uses `actions/cache@v4` keyed on `hashFiles('src/flying_probe_copilot/generator/cli.py', 'src/flying_probe_copilot/generator/**', 'src/flying_probe_copilot/parser/cli.py', 'src/flying_probe_copilot/parser/**', 'scripts/build-portfolio-data.sh', 'uv.lock')` (B-5 non-glob anchors) with `restore-keys: sample-duckdb-` fallback; cache-miss runs `bash scripts/build-portfolio-data.sh`; then `playwright install --with-deps chromium` + `scripts/capture_screenshots.py all --db data/db/sample.duckdb --out docs/img-ci/`; uploads `docs/img-ci/` as `actions/upload-artifact@v4` `recaptured-dashboard-screenshots` (14-day retention) per D2. Owner-approved approval-gated `pyproject.toml` edit: `ruff>=0.6` to `[dependency-groups].dev` + `[tool.ruff]` block (`line-length=100`, `target-version="py311"`, `extend-exclude=["data","docs","notebooks",".claude","scripts"]` per D16/B-4) + `[tool.ruff.lint].select=["E","F","W","I"]` + `[tool.ruff.lint.per-file-ignores]."tests/**/*.py"=["E501","F401"]` (W-8 explicit glob) + `[tool.ruff.format]` defaults. Lint preflight surfaced 301 ruff errors + 58 unformatted files in existing code → owner ratified D17 = **Option A cleanup pass** as one-time slice-3 exception to the read-only `src/**` guardrail. Cleanup commit `056459e` applied `ruff check --fix` (189 auto-fixes) + `ruff format` (58 files) across 26 src/ + 42 tests/ files (+1517/-1126), then 7 D17.1 hybrid suppressions: 4 F841 per-line `# noqa` (intent-marker locals), 2 E712 per-line `# noqa` (pandas `.loc` idioms), 1 E741 refactor `l` → `shift_val`/`line_val` in test_ingest.py, plus 11 inline `import duckdb  # noqa: F811` adds in `test_views_smoke.py` (load-bearing for `AppTest.from_function` subprocess execution). New `tests/test_ci/` package (3 files): `__init__.py` + `conftest.py` (4 session-scope fixtures with `copy.deepcopy` per W-6) + `test_workflow_yaml.py` (83 `def test_*` + 9 parametrize decorators = **93 collected** tests covering YAML shape, path-filter contracts, no-secrets defense-in-depth, pyproject ruff config). **Suite: 659 passing / 5 skipped / 1 xfailed / 97% coverage** (baseline 566/5/1 → +93 ci tests; src/ coverage unchanged because new tests don't import src/). Zero `.claude/**` edits. Zero `scripts/**` edits. Step 5 red-team caught 7 BLOCKERs (B-1 PyYAML `on:` → `True` keyword trap → quote `"on":`; B-2 `python-version` float coercion → quoted `"3.11"` callout; B-3 `setup-uv@v3` three majors stale → @v8; B-4 `scripts/` missing from `extend-exclude` → added per D16; B-5 cache-key `hashFiles(**)` collapse-to-constant on zero match → `cli.py` non-glob anchors; B-6 `--all-groups` flag on stale uv → `version: ">=0.7"` floor; B-7 ruff format preflight policy → owner-decisioned D17/D17.1) + 12 WARNs + 8 MISSING-DECISIONs — all closed in Plan-Rev1 before Execute. All 4 D17 BLOCKER-class decisions ratified at "Recommended on all". Branch: `feature/phase4-slice3-ci-workflows` (renamed from worktree `claude/sweet-jones-7291db` at Step 10). **Next: Phase 4 slice 4 — portfolio promotion (final guardrails audit + repo public flip + docs/DEMO.md + blog post + LinkedIn post + resume bullet + branch-protection rules).** Slice 2 (headless screenshot capture + demo gif, 2026-06-21): merged via PR #34. New `scripts/capture_screenshots.py` (400 LOC, Playwright + Pillow) + `scripts/_capture_app.py` (32 LOC Streamlit shim that monkeypatches `chat.answer_question` to a canned grounded `Answer` over `failure-modes/tombstoning.md#3`) regenerate all 6 dashboard JPGs + a 748 KB `docs/img/demo.gif` from one command against the gitignored 900-panel `data/db/sample.duckdb` — no live Gemini key needed. Co-Pilot screenshot now shows the BUG-014 narrative artifact (canned tombstoning answer with the Citations expander **opened** showing `failure-modes/tombstoning.md#3`). README embeds the gif above the hero strip. Owner-approved approval-gated edits: `pyproject.toml` (×2: `playwright>=1.49` in dev group + `[tool.pytest.ini_options].pythonpath = [".", "src"]` so `from scripts import` resolves in tests) + `README.md` (gif embed). New `tests/test_scripts/`: 38 unit + 4 shim = 42 green; 5 env-gated end-to-end correctly skipped. `ui_db_path` fixture lifted from `test_ui/conftest.py` → `tests/conftest.py` per MD-3. Declared parallel edit `tests/test_ui/test_chat_smoke.py:24` `#0`→`#3` (B-1 ripple). **Suite: 566 passing / 5 skipped / 1 xfailed / 97% coverage** (baseline 524/3/1 → +42/+2). Zero `src/flying_probe_copilot/**` edits. Zero `.claude/**` edits. Step 5 red-team caught 5 BLOCKERs (B-1 wrong citation chunk #0=title vs #3=Likely-causes; B-2 `st.expander` defaults collapsed; B-3 pytest pythonpath; B-4 monkeypatch survival; B-5 emoji-prefixed sidebar nav) — all closed in Plan-Rev1 before Execute. Live-capture iteration surfaced 2 issues red-team missed: `st.expander` renders as `<details><summary>` not `<button>` (selector fix), and Overview's twin-column charts need 4 s initial settle. Branch: `feature/phase4-slice2-screenshots`. **Next: Phase 4 slice 3 — GitHub Actions workflow (lint + tests + screenshot-recapture-on-PR) + repo public flip.** Slice 1 (README polish + portfolio writeup, 2026-06-21): Phase-0 README replaced with a portfolio-grade rewrite: hero strip of 6 dashboard screenshots, Mermaid architecture diagram, 7-step Quickstart, status table, About-the-author footer. `docs/case-study.md` (~2,100 words) walks problem → scope decisions → architecture → three engineering stories (BUG-004 / BUG-013 / BUG-011) → RAG design choices → verified results table → honest retrospective. Phase 3 officially shipped (PR #29 merged to main 2026-06-21; live ≥8/10 eval PASSED 10/10 in 37.13s on `gemini-3.5-flash`; strengthened later same day to ≥12/15 — PASSED in 75.06s — after BUG-014 fixed the terse-query retrieval miss). Phase 4 carry-over chips all closed in parallel on dev 2026-06-21: SDK migrate `google-generativeai` 0.8.6 → `google-genai` 2.9.0 (PR #30 merged); BUG-010 + BUG-012 (PR #31); BUG-014 (PR #33). (Phases 0/1a/1b/2/3 complete.)

## Hard guardrails (NEVER violate)

1. **No real customer log data ever enters this repo or any home machine.** Real-data validation happens only on the work network. See `docs/GUARDRAILS.md`.
2. **No IPC-A-610 / J-STD-001 verbatim text in the repo.** Copyrighted. Use summaries and citations only.
3. **No proprietary Keysight documentation copied wholesale.** Reference public manual sections by name; don't redistribute.
4. **All committed data is synthetic.** The synthetic generator is the single source of test data for the repo.
5. **API keys never committed.** Use `.env` + `python-dotenv`. `.env` is gitignored.

## Phase-based workflow

This project ships in 4 phases. Each phase has its own focus, deliverable, and ends with a written status update in `docs/ROADMAP.md`.

| Phase | Goal | Status |
|---|---|---|
| 0 — Setup | Docs, scope, guardrails, repo skeleton | ✅ Complete |
| 1a — Synthetic data | HP3070-style log generator | ✅ Complete (8/9 — README deferred) |
| 1b — Parser & DB | Log parser + DuckDB schema | ✅ Complete (notebook deferred) |
| 2 — Analytics | SPC, Pareto, anomaly detection, dashboard v1 | ✅ Complete (slices 1+2+3: analytics + Streamlit dashboard) |
| 3 — RAG co-pilot | Hybrid RAG over results + failure-mode KB | ✅ Code complete (slice 1 retrieval + 2 Gemini answer layer + 3 chat UI/eval; live ≥8/10 = owner env-gated run) |
| 4 — Polish | Tests, docs, portfolio writeup, demo gif | ⬜ |

**Rule: One phase per session.** If you're in Phase 1a, do not start writing parser code. Park ideas in `docs/DECISIONS.md` or a phase note.

## Tech stack (locked)

- Python 3.11+
- `uv` for dependency management
- DuckDB for SQL spine
- ChromaDB + sentence-transformers + rank-bm25 for hybrid RAG
- Google Gemini API (primary LLM); Claude API (backup)
- Streamlit + Plotly for UI
- pytest for tests

## Workflow conventions

- **Multi-IDE:** Claude Code = explore/plan/debug/doc. Cursor = primary builder. Both read this file.
- **Subagents:** For exploration spanning >3 queries, spawn an Explore subagent. For non-trivial implementation, spawn a Plan subagent first. Default to pipeline over parallel.
- **Adversarial verification:** Before acting on any finding, spawn 2-3 skeptic subagents to refute it.
- **Pre-fanout check:** Always read this file + the relevant phase's spec/doc before any multi-agent work.
- **Trust but verify:** Subagent summaries describe intent, not always what landed. Spot-check.
- **Use Context7 MCP** for live documentation lookup of DuckDB, Streamlit, ChromaDB, sentence-transformers, Gemini SDK.

## Reference paths

- Local repo: `E:\flying-probe-copilot\` (to be created)
- Personal Assistant home base: `E:\Personal Assistant\`
- Related projects: `E:\Portfolio\`, `E:\my-assembly-hub\`
- Cross-project findings → surface via `spawn_task` chips, don't silently scope-creep.

## Don't-do list (project-specific)

- ❌ Don't train a custom model. Use pre-trained embeddings + LLM API.
- ❌ Don't build a generic test-log parser. Target HP3070 / Keysight i3070 format first; expand later.
- ❌ Don't add MCPs beyond Context7 in Phase 0-2. Reassess in Phase 3.
- ❌ Don't write production-grade error handling in Phase 1. Get the happy path working first.
- ❌ Don't optimize prematurely. DuckDB on synthetic data is fast enough for any v1 query.
- ❌ Don't add a real frontend (React/Next). Streamlit only.
- ❌ Don't deploy to cloud in v1. Local + GitHub repo is the deliverable.

## Definition of "done" for each phase

- Phase 0: Every doc in `/docs` exists and is reviewed. Repo initialized on GitHub.
- Phase 1a: Generator produces ≥3 realistic HP3070-style log variants from configurable parameters; unit tests pass.
- Phase 1b: Parser ingests all generator output into DuckDB; query "yield by board over last week" returns correct results.
- Phase 2: Streamlit dashboard shows yield-over-time, failure Pareto, and anomaly flags on synthetic data.
- Phase 3: Co-pilot answers ≥10 representative root-cause questions correctly with citations to retrieved rows.
- Phase 4: Public GitHub repo with README, demo gif, case-study writeup on portfolio site.

## Open questions (update as we go)

- [ ] Exact field set for HP3070 log format — refine in Phase 1a spec
- [ ] Whether to include digital test patterns or only analog/shorts in v1
- [ ] Whether to ship an "import real logs" tool alongside the synthetic generator (Phase 4)

## Session log

Add a line each session: `YYYY-MM-DD — <phase> — <what was done>`

- 2026-06-13 — Phase 0 — Repo on GitHub; full .claude/ governance layer (10 skills incl. skill-sergeant, 3 hooks, rules); 10-step multi-agent loop finalized; hrk-agent-starter portable kit built and pushed; branching confirmed (feature/* → dev → main). Remaining: pyproject.toml + Keysight manuals.
- 2026-06-13 — Phase 0: 8/9 done — pyproject.toml committed. Keysight manuals NOT downloaded (off-git owner task, still pending). HP3070 log format will be researched from public sources in Phase 1a Explore step instead.
- 2026-06-13 — Phase 1a — synthetic HP3070 generator complete in single session. Format target revised mid-session to real Keysight Log Record Format (authoritative reference via Virinco public mirror). 9 source modules + 11 test files + conftest = 81 tests, 94% coverage, ~1 s for 1000 panels. 10-step workflow loop (brief → 2-explore → plan → red-team → execute → verify → triple-check → docs/commit) ran clean. Parent Step 7 corrected 2 contract drifts found by Step 6. uv installed standalone. BUG-001 logged for cache-research subagent process improvement. Next: Phase 1b parser + DuckDB schema.
- 2026-06-13 — Phase 1a prep — broadened `data/synthetic/` `.gitignore` to a samples-only allow-list so bulk generator outputs (20–50 MB results.csv/json from 1k-panel runs) can't accidentally be committed; added `data/synthetic/samples/.gitkeep`. Branch: feature/gitignore-data-synthetic-v2. (`uv.lock` un-ignore is a separate concern, already landed on branch fix/commit-uv-lock.)
- 2026-06-14 — Phase 1a meta — added dedicated `exec` sub-agent (`.claude/agents/exec.md`, sonnet, tool-restricted) + 3 workflow templates (`sub-agent-brief.md`, `tiering.md`, `prompt-caching.md`). Formalizes tier-based step selection (Trivial / Small / Medium / Large) and context-cache brief reuse to cut ~30–50% input tokens on Medium/Large loops. No source code touched. Branch: feature/exec-agent-and-templates.
- 2026-06-14 — Phase 1a fix — wired `correlation_multiplier` into `generate_blocks`. Bugbot caught (PR #3 review, medium severity) that the refdes-numerical clustering heuristic existed in `faults.py` but was never invoked from the CLI output path — `_pick_failing_component` marked exactly one component per panel. Added `_pick_correlated_failures` helper with multiplier-gated Bernoulli draws (`BASELINE_SECONDARY_RATE=0.3`, applied only when multiplier > 1.0) + 3 new tests (within-panel, aggregate Pareto, cross-family). All 97 tests pass. Branch: feature/wire-fault-correlation.
- 2026-06-14 — Phase 1a test fix — closed the coverage gap Bugbot flagged in PR #3 (comment 3409766434). `tests/test_generator/test_lexical_compliance.py` built panels with a hardcoded 4-block fixture (the pre-BUG-002 shape), so after the BUG-002 fix it never exercised the real CLI block-generation path (`generate_blocks`, 51 / 201 / 801 blocks per panel). Rewrote the test to mirror `cli._build_batch_log` exactly, added a `large`-profile case + a regression guard requiring ≥`component_count + 1` blocks per board. Lexical assertion now spans ~2,376 emitted blocks (was ~152). 98/98 tests pass, 94% coverage held. Branch: feature/lexical-test-via-generate-blocks.
- 2026-06-14 — Phase 1b — Parser & DuckDB schema complete in single Large-tier session (full 10-step loop). New: `src/flying_probe_copilot/parser/` (4 files: `__init__.py`, `log_parser.py`, `ingest.py`, `cli.py`) + `src/flying_probe_copilot/db/schema.py` (9 tables: 5 dim + runs + 3 fact, idempotent `CREATE TABLE IF NOT EXISTS`) + 7 new test modules under `tests/test_parser/`. Single-line `pyproject.toml` edit re-added `parser` script. **179 tests passing** (98 generator baseline + 81 parser), 0 failing, **97% total coverage** (parser 97%, db 100%, generator baseline preserved). Round-trip integrity test (generator → parser → DuckDB → query) confirms count + timestamp equality at byte/IEEE-754 precision. "Yield by board over last week" query passes the 2-week × 2-profile fixture exit-criterion test. Loop ran clean: brief → explore → plan (v1 + Revision 1 resolving 2 BLOCKERs + 5 WARNINGs + 6 MINORs from Step 4 red-team) → exec → independent verify (PASS) → triple-check (CLEAN). Notebook deliverable (`notebooks/01-queries.ipynb`) and per-panel operator-ID recovery deferred to follow-up sessions (chips at Step 8). Branch: feature/phase1b-parser.
- 2026-06-14 — Phase 1a fix — closed the shift-snap overnight bug Bugbot flagged in PR #3 (comment 3409766436, low severity). `generate_panel_schedule` drew a shift letter uniformly per panel and snapped to the raw draw's calendar day; the shift-C wrap correction was a literal `pass`, so a raw_ts at 02:00 randomly assigned to shift C jumped to the same day's 22:00–05:59 window. Adopted option A: derive `shift` from `ts.hour`, snap inside the window-start that physically contains the raw draw (previous-day 22:00 for early-morning shift C). New helpers `_shift_for_hour` + `_shift_window_start`; old `_shift_start_for` deleted as dead code. Logged BUG-004. 3 new tests (RED-first targeted regression + 2 contract checks). 101/101 tests pass, 95% total coverage. Branch: feature/fix-shift-snap-overnight.
- 2026-06-14 — Phase 1b — Notebook deliverable closed (`notebooks/01-queries.ipynb`, nbformat 4.5, 17 cells = 9 markdown + 8 code) — canonical yield-by-board-last-7-days query (CTE anchored to `MAX(panels.scheduled_ts)` for deterministic replay) + 5 analytics queries: failure Pareto by record_type, per-shift yield, per-operator yield, top-10 failing refdes, btest_status distribution. Sample DB `data/db/sample.duckdb` ingested from a 20-panel small-profile run (gitignored via `*.duckdb`). Every code cell smoke-tested in-process against the live DB. ROADMAP Phase 1b now 7/7. Tier: Small (doc-only). Branch: feature/phase1b-notebook. Side note: mid-session `cd notebooks/` exposed a hook-config sharp edge — `.claude/settings.json` registers `python .claude/hooks/block_dangerous_git.py` with a relative path, which hard-blocks every shell command when the cwd drifts into a subdir. Surfaced as a `spawn_task` chip for a separate owner-approved settings.json edit.
- 2026-06-14 — Governance fix — flipped all three hook commands in `.claude/settings.json` from relative paths to `python ${CLAUDE_PROJECT_DIR}/.claude/hooks/<file>.py` (Option A — portable, owner-approved over hard-coded path Option B). Same fix stamped upstream into `E:\hrk-agent-starter\.claude\settings.json` so every future stamped project gets it. Smoke-tested in-session: `cd notebooks && pwd && cd ..` succeeded immediately after the edit, proving the harness substitutes `${CLAUDE_PROJECT_DIR}` on this Windows machine. DECISION_LOG entry documents A-vs-B rationale, what was rejected, and revisit condition. Branch: feature/abs-hook-paths. Tier: Small (config + docs only).
- 2026-06-16 — Phase 2 first task — per-panel operator-id repair (Path A) landed on `feature/per-panel-operator`. `@BTEST` extended with mandatory `operator_id` at field 12; wired end-to-end through models → CLI → renderer → grammar → parser → ingest; `test_runs.operator_id` flipped to `VARCHAR NOT NULL`. 11 new tests, 196 passing total, 0 failing, 97% coverage (schema 100%, parser 97%, generator ≥90%). BUG-009 resolved; BUG-007 operator half closed (shift + line_id remain open); BUG-010 logged for TestJetRecord pytest collection warning. DECISION_LOG 2026-06-14 nullable-operator entry footnoted "Resolved 2026-06-16 — Path A landed". Notebook Query 4 caveat closed. Tier: Medium. 12-step workflow loop ran clean — plan was authored under prior 10-step workflow and migrated cleanly (red-team Revision 1 = new Step 5 Verify Plan; embedded per-step RED test cases covered new Step 4 Test-Case Plan).
- 2026-06-16 — Phase 2 — slice 1 (analytics module foundation) complete in a single Medium-tier session. `src/flying_probe_copilot/analytics/` shipped with two library functions: `yield_over_time(con, *, window_days=7, group_by="board", as_of=None) → list[YieldRow]` and `failure_pareto(con, *, window_days=7, by="record_type", top_n=10, as_of=None) → list[ParetoRow]`. 39 new tests (17 yield + 19 pareto + 3 public API), **224/224 passing**, 0 failing, analytics package coverage 96-100% per file. Zero edits to existing tracked files (pure additive). Zero new dependencies (pure stdlib + already-present duckdb). Six v1 contracts documented in DECISION_LOG: Pareto v1 groups by record_type only (notebook Q2's 2-column key deferred); yield ordering `group_key ASC` universally (diverges from notebook Q4); percentages are unrounded floats; `window_days <= 0` / `top_n <= 0` / tz-aware `as_of` all raise `ValueError`. Per-row `placeholder_fields: tuple[str, ...]` marker keeps BUG-007-affected columns visible on every code path that groups by shift / line_id / operator_id — silent placeholder data is now impossible. Full 12-step session-workflow loop ran end-to-end; Step 5 adversarial review caught 7 BLOCKERs (notebook Q2 grouping, tiebreak ordering claims, yield_pct rounding, fixture under-specification, empty-anchor short-circuit, cross-fixture-import constraint) that Plan Revision 1 resolved before Execute. Branch: feature/phase2-analytics-foundation (renamed at brief time from worktree's `claude/quizzical-neumann-ba99a3`). Slice 2 (SPC + anomaly), slice 3 (Streamlit dashboard), and BUG-007 proper fix all deferred to follow-up sessions.
- 2026-06-17 — Phase 2 — BUG-007 fully closed in a fast follow-up commit on `feature/per-panel-operator`. Path A applied to the remaining two fields: `@BTEST` gains mandatory `shift: Literal["A","B","C"]` at field 13 and `line_id: str = Field(min_length=1)` at field 14; wired through models → CLI → renderer → grammar → parser; `_make_board_log` reads `btest.shift` / `btest.line_id` instead of the `"A"` / `"LINE-A"` placeholder literals. Schema was already `NOT NULL` for both columns so no schema flip — the bug was silent-wrong-data, not nullability. 4 new tests (3 model-layer guards + multi-shift/multi-line end-to-end ingest); 200 passing total, 0 failing, 97% coverage. Bulk-patch of 12 `BoardTestRecord(...)` blocks + 30 hardcoded `@BTEST|` literals. Notebook Query 3 caveat closed. Per-shift + per-operator + per-line analytics now all sit on real per-panel data. Tier: Small (mechanical mirror of operator_id pattern, no separate brief/plan — TDD discipline preserved). PR `feature/per-panel-operator` → `dev` now closes both halves of BUG-007 in one PR.
- 2026-06-18 — Phase 2 — dropped the stale `placeholder_fields` markers from the analytics dataclasses now that BUG-007 is closed. Field removed from `YieldRow` + `ParetoRow`; `_GROUP_BY_CONFIG` collapsed from a 3-tuple to a 2-tuple; placeholder-specific tests (Y-08, P-12, P-13) retired; Y-09/Y-10/Y-11 refactored into plain group_by smoke tests on real shift/line/operator data; Y-12 xfail reason cleaned up. DECISION_LOG 2026-06-18 documents the breaking-change rationale and footnotes the 2026-06-16 marker decision as "Resolved". 235 passing, 1 xfailed, 97% coverage. Branch: feature/analytics-drop-placeholder-markers. Tier: Small. Resolves chip task_3cf21775.
- 2026-06-18 — Phase 2 slice 2 — SPC + anomaly analytics shipped as pure library functions. `analytics/spc.py::individuals_chart` (Shewhart individuals/XmR chart; Wheeler doctrine: default rule_1 [beyond 3σ] + rule_4 [run of 8], opt-in rule_2/rule_3 zone rules; sigma = MR̄/1.128 exact, never sample stdev; value = per-panel mean(measured_value) for a (board_profile_id, refdes) via a components join) + `analytics/anomaly.py::z_score_anomalies` (per-group failure-rate, leave-one-out baseline [ddof=1, group excluded from its own mean+std], two-sided |z|≥threshold, severity-first ordering). 57 new tests (29 SPC + 24 anomaly + 4 API), 292 passing / 1 xfailed / 0 failing, spc.py+anomaly.py 100% coverage, repo 97%. Notebook Query 7 + Query 8 added + smoke-tested. Full 12-step loop: Step 2 = 5-agent research/map/synthesis workflow (Western Electric/Nelson/Wheeler SPC-rule survey); Step 4+5 = test-generator (50 cases) → red-team (1 BLOCKER [2.66 vs 3/1.128 precision trap] + 4 WARNING [refdes lives on components not measurements; stdev raises on 1 peer] → Plan Revision 1); Step 6 Decision Gate owner-ratified 4 choices (Wheeler rules / refdes selector / failure-rate metric / defer X-bar/R + Isolation Forest); Step 9 Triple Check CLEAN. Zero new deps, zero schema change, zero approval-gated-file edits. BUG-011 logged (pre-existing flaky parser test). Branch: feature/phase2-slice2-spc-anomaly. Tier: Large.
- 2026-06-18 — Phase 2 — fixed flaky `test_tokenize_balances_braces_returns_records` (BUG-011). Root cause was test-only shared mutable state, not production code: the `_render_to_text` helper in `tests/test_parser/test_log_parser.py` rendered to a single fixed path at the worktree root (`tmp_test_render.log`), shared by both its callers and exposed to repo-tree file-watcher/AV locks on Windows — so it passed in isolation but failed intermittently in the full suite (partial-read `AssertionError` or `PermissionError [WinError 32]`). Fix: switch the helper to pytest's per-test `tmp_path` (unique + under OS temp), drop the manual unlink, thread `tmp_path` through both callers; added regression test `test_render_helper_isolates_to_tmp_path_not_repo_root`. No production-code / approval-gated files touched. Reproduced before/after with a two-thread stress harness (shared path 484/800 failures → isolated 0/800) plus a live concurrent-sweep run (old code failed, fixed code passed). 236 passing, 1 xfailed, 97% coverage. BUG-011 logged + RESOLVED (was missing from BUG_LOG despite the brief referencing it). Branch: claude/beautiful-beaver-e50f90. Tier: Small.
- 2026-06-18 — Phase 2 slice 3 — **Streamlit dashboard shipped; Phase 2 COMPLETE.** New `src/flying_probe_copilot/ui/` (5 files: `app.py` `st.navigation` entry + `data.py` cached-query/pure-helper layer + `charts.py` pure Plotly builders + `views.py` 5 page renderers + `__init__`) over the 4 analytics functions — Overview/Yield/Pareto/SPC/Anomalies pages, date-range + dimension + value filters, `st.cache_data` results on a read-only `st.cache_resource` DuckDB connection. 81 new tests (`tests/test_ui/`: pure-helper + chart-builder units + `AppTest` view/app smoke incl. empty/no-board/missing-DB branches); **373 passing / 1 xfailed / 97% coverage** (`ui/data.py`+`ui/charts.py` 100%). Live `streamlit run` boots clean; default page renders in **0.23 s** (exit < 2 s). Zero existing-file edits, zero approval-gated edits (streamlit+plotly were already in `pyproject.toml`+`uv.lock` → the anticipated gated dep-add was moot), analytics layer unchanged. Owner Decision Gate ratified: yield=bar-per-group (not time-series; `day` deferred), work on worktree branch `claude/zen-roentgen-2818ce` → PR to `dev`. BUG-012 logged (`use_container_width` deprecation; fix needs gated `pyproject` floor bump). Full 12-step Medium loop: `/skill-sergeant`→`/frontend-design`→`/plan-architect`→Decision Gate→`exec` TDD→parent Triple Check (live launch verified). Branch: claude/zen-roentgen-2818ce. Tier: Medium.
- 2026-06-20 — Phase 2 → Phase 3 — flushed all pending Phase 2 slices to main. Merged slice 2 (SPC + anomaly, PR #16 → dev, PR #18 slice 3 Streamlit → dev, PR #19 dev → main). Phase 2 fully closed. Phase 3 (RAG co-pilot) starts next session.
- 2026-06-20 — Phase 3 slice 1 — offline hybrid-retrieval core + KB scaffold. `src/flying_probe_copilot/rag/` (6 files): `models` (Chunk/RetrievedChunk frozen), `kb_loader` (fence-aware heading chunking, deterministic POSIX-relpath ids), `lexical_index` (BM25Okapi, token-overlap match), `vector_index` (in-memory chroma `hnsw:space=cosine`, injectable lazy embedder, all-zero guard), `retriever` (RRF k=60, sort score DESC then chunk_id ASC; `build_retriever`), `__init__` (7 public names). `docs/knowledge-base/` seeded with README + 00-index + 8 synthetic failure-mode docs (standards by section number only — guardrail-compliant). 80 new tests (`tests/test_rag/`, model-free FakeEmbedder), **454 passing / 1 xfailed / 97% coverage**, rag 99–100% per file; fully offline + deterministic (real all-MiniLM-L6-v2 env-gated `RAG_RUN_MODEL_TESTS`). Full 12-step Large loop; Step 5 red-team caught 3 BLOCKERs (chroma L2-vs-cosine, RRF non-universal both>one claim, BM25 ≤0-score match) → Plan Revision 1 before Execute. Zero approval-gated edits (deps already declared+locked). Branch: `feature/phase3-slice1-rag-retrieval`. Tier: Large. **Next: slice 2 — Gemini LLM + citation prompt (needs owner's Gemini API key).**
- 2026-06-20 — Phase 3 slice 2 — Gemini LLM answer layer. `rag/llm.py` (`LLMClient` runtime_checkable Protocol + `GeminiClient`: lazy, `_resolve_key` from api_key/`.env`/env, missing→`ValueError`, live `_call_model` lazy-imports google-generativeai 0.8.6 + `# pragma: no cover`), `rag/prompts.py` (`build_answer_prompt` — citation-forcing JSON instruction), `rag/answer.py` (`Answer` frozen + `answer()` with strict anti-hallucination grounding: refuse unless hits + valid JSON + `sufficient is True` + non-empty answer + ≥1 retrieved citation; LLM never called on blank/no-hit; hallucinated citations dropped; `REFUSAL_TEXT`), `rag/__init__` → 11 public names. 42 new tests (`tests/test_rag/`: autouse env-strip + FakeLLMClient/RaisingLLMClient/StubRetriever), **496 passing / 1 xfailed / 97%**, new modules 100%; fully offline + secret-safe (no live API in suite; lazy SDK import → no FutureWarning on `import rag`). Full 12-step Large loop; Step 5 red-team caught 2 BLOCKERs (`load_dotenv` injecting the real `.env` key into the test process; `test_api03` exact-set `__all__` break) → Plan Revision 1 before Execute; one declared slice-1 test edit (`test_public_api` __all__ → 11 names). Zero approval-gated edits. Branch: `feature/phase3-slice2-llm`. Tier: Large. ⚠️ **Owner: rotate the `.env` GOOGLE_API_KEY (surfaced in a subagent).** **Next: slice 3 — chat UI in Streamlit + live 10-Q ≥8/10 eval.**
- 2026-06-20 — Phase 3 slice 3 — chat UI + 10-Q eval; **Phase 3 code complete.** `ui/chat.py` (`render_chat` — `st.chat_input` → module-global `answer_question` → render answer + citations in `st.expander`; refusal renders `REFUSAL_TEXT`; backend errors → `st.error` no-append; live seams `get_retriever`/`get_client`/`answer_question` `# pragma: no cover`). `ui/app.py` +6th "Co-Pilot" `st.Page` (declared edit, docs→6 pages). `tests/test_rag/eval_dataset.py` (`EVAL_QUESTIONS` 10 Q over all 8 KB docs) + `docs/eval/phase3-eval-questions.md`. ~23 new tests: `tests/test_ui/test_chat_smoke.py` (6 AppTest via self-contained `_smoke_chat` wrapper + monkeypatched backend), `tests/test_rag/test_eval.py` (dataset integrity + 10 offline citation-pattern + hallucinated-cite/off-domain refusal + env-gated live ≥8/10). Autouse env-strip added to `tests/test_ui/conftest.py`. **519 passing / 1 skipped (live eval) / 1 xfailed / 97%**, `ui/chat.py` 100%; offline + secret-safe. Full 12-step Large loop; Step 5 red-team caught 1 BLOCKER (`AppTest.from_function(render_chat)` can't run a real module fn → self-contained wrappers + module-global patch). Zero approval-gated edits (declared `app.py` edit). Branch: `feature/phase3-slice3-chat-ui`. Tier: Large. **Exit criterion = owner's `RAG_RUN_LLM_EVAL=1` live ≥8/10 run + `dev→main` promotion. Next: Phase 4 — polish & portfolio.**
- 2026-06-21 — Phase 3 exit-criterion run + model bump (BUG-013). Live `RAG_RUN_LLM_EVAL=1` first attempt failed in 2m31s with `google.api_core.exceptions.NotFound: 404 This model models/gemini-2.0-flash is no longer available` — Google retired the model (Context7 `/websites/ai_google_dev_gemini-api` lists 2.0 Flash under "Previous models, now shut down"). The 2:31 wall-clock was entirely the gRPC client's 600s deadline + retries, not real API work. Bumped `DEFAULT_GEMINI_MODEL` `"gemini-2.0-flash"` → `"gemini-3.5-flash"` (current flash tier) at three sites: `src/flying_probe_copilot/rag/llm.py:18` + `tests/test_rag/test_llm.py:23` + `.env.example:14` (owner-approved approval-gated edit). Offline LLM tests 4/4 green; live eval re-ran → **PASSED 10/10 in 37.13s**. **Phase 3 exit criterion MET.** BUG-013 logged + RESOLVED. Phase 4 chip: migrate deprecated `google-generativeai` 0.8.6 SDK → `google-genai` (FutureWarning emitted on import; cf. `llm.py:32`). Branch: claude/tender-pascal-30e50a. Tier: Small.
- 2026-06-21 — Phase 4 slice 1 — **README polish + portfolio writeup**. Full README rewrite replacing the stale Phase-0 wording: shields-row (Phase 3 shipped / 519 tests / 97% / 10/10 eval / MIT), 2×3 hero strip of 6 dashboard screenshots (Overview/Yield/Pareto/SPC/Anomalies/Co-Pilot under `docs/img/`), Mermaid architecture diagram (quoted-label syntax — Plan Rev 1 fix for B-1; renders inline on GitHub), 60-second elevator, 7-step Quickstart (incl. the parser stamped-run-dir gotcha fixed in Plan Rev 1 B-2), tech stack table, project-structure tree, doc-map table, status-and-roadmap table (Phase 3 shipped 2026-06-21), Contributing/License, About-the-author footer linking LinkedIn + portfolio site (owner-provided URLs). New `docs/case-study.md` (~2,100 words, 7 sections): problem framing (PCBA/ICT logs landscape), scope decisions (synthetic-only / local-first / Streamlit / hybrid RAG / KB-grounding-not-DuckDB — W-1 narrative guardrail), architecture walk, **three engineering stories** (BUG-004 shift-snap overnight, BUG-013 model retirement diagnosis, BUG-011 flaky test under parallel load), RAG design (hybrid retrieval + strict anti-hallucination contract + eval-as-code), verified results table (519 / 97% / 10/10 / 37.13s / ~1s/1k panels / **78 commits / 29 PRs** verified live, not the placeholder 97/28 — W-3), honest retrospective. `docs/ROADMAP.md` Phase 4 deliverables ticked for README + case-study (per Decision Gate item 7). Full 12-step reduced Medium loop ran clean: brief → Explore subagent (What/Why/Where/When map) → Plan (parent) → Verify Plan red-team subagent (3 BLOCKERs + 5 WARNINGs + 3 MINORs + 3 missing decisions → Plan Revision 1 closed all of them) → Decision Gate (4 owner decisions ratified at Recommended; 4 parent pre-decided) → Execute (parent, docs-only) → screenshots captured by owner against live `streamlit run` on :8501 with live Gemini key. Zero code touched, zero approval-gated edits (declared CLAUDE.md/ROADMAP.md per agent-conduct line 64). Existing suite still 519 / 1 xfailed / 97%. Branch: feature/phase4-slice1-readme. Tier: Medium (reduced loop, no tests). **Next: Phase 4 slice 2 — demo gif / CI screenshot capture (auto-recapture from headless Playwright).**
- 2026-06-21 — Phase 4 chip: SDK migrate `google-generativeai` 0.8.6 → `google-genai` 2.9.0. Single touchpoint per BUG-013 follow-up note. Confirmed new SDK shape on PyPI (`from google import genai`; `client = genai.Client(api_key=...)`; `client.models.generate_content(model=..., contents=..., config=types.GenerateContentConfig(response_mime_type="application/json")).text`). Owner-approved approval-gated `pyproject.toml` edit: `google-generativeai>=0.8` → `google-genai>=1.0` (broad floor; lockfile pinned to 2.9.0 via `uv sync`). `src/flying_probe_copilot/rag/llm.py:_call_model` body swapped to the new client API (still `# pragma: no cover - live API`), module + class docstrings updated. Model id (`gemini-3.5-flash`) and public Python API unchanged. Offline LLM contract suite (LLM-01..05b) still green by construction — those tests cover lazy construction + key resolution + Protocol conformance, none of which change. Full suite: **519 passing / 1 skipped (live eval) / 1 xfailed / 97% — identical to the pre-migration baseline.** Warnings audit clean: no `google.generativeai` FutureWarning, no `google.genai` warnings (3 pre-existing transient warnings: opentelemetry + 2 × TestJetRecord/BUG-010). **Live `RAG_RUN_LLM_EVAL=1` ≥8/10 eval re-ran on the new SDK in 166.20s and PASSED** (single invocation; cold sentence-transformers + Chroma bootstrap accounts for the delta vs the prior warm 37.13s — the model call itself is unchanged). BUG-013 follow-up note closed. Branch: feature/sdk-migrate-google-genai (merged into `dev` as `e4e9a0a` via PR #30). Tier: Small-to-Medium.
- 2026-06-21 — Phase 4 P3 cleanups: BUG-010 (`TestJetRecord` PytestCollectionWarning) + BUG-012 (Streamlit `use_container_width` deprecation) closed in a single Small-tier session. BUG-010 fix: added `__test__ = False` class attribute on `TestJetRecord` at `src/flying_probe_copilot/generator/models.py:329` — pytest's documented per-class opt-out; dunder is invisible to Pydantic v2's field-detection metaclass so `ConfigDict(extra="forbid")` is unaffected. BUG-012 fix: owner-approved approval-gated `pyproject.toml` edit (`streamlit>=1.40` → `streamlit>=1.45`; uv.lock unchanged on 1.58.0), then `replace_all` across `src/flying_probe_copilot/ui/views.py`'s 10 call sites (`use_container_width=True` → `width="stretch"`; all 10 sites were `=True`, no `width="content"` substitution needed). Full suite **519 passed / 1 skipped / 1 xfailed / 97% — baseline held**; warnings dropped 3 → 1 (only the unrelated opentelemetry transitive `SelectableGroups` remains). All three Phase 4 chips from the 2026-06-20/2026-06-21 Phase 3 wrap-up now closed in code. Branch: feature/p3-cleanups-bug-010-012. Tier: Small.
- 2026-06-21 — Phase 4 polish — BUG-014 RAG retrieval miss for terse "what causes X?" queries fixed. Portfolio screenshot capture surfaced that "what causes tombstoning?" in the Co-Pilot chat returned a refusal because `failure-modes/tombstoning.md#3` ("Likely causes") fell out of the `top_k=5` cut — empirical probe revealed the chunk is at rank **9** (generic vocabulary in the body + "causes" appearing in every doc's "Likely causes" heading dilutes both BM25 and vector signals). Brief proposed `top_k=8`, owner re-ratified at second Decision Gate after probe data showed 8 still misses (rank 9 > 8). Fix: extracted `DEFAULT_TOP_K = 10` constant in `src/flying_probe_copilot/rag/answer.py`, expanded `EVAL_QUESTIONS` 10 → 15 (5 terse regression questions appended), live-eval threshold scaled 8/10 → ≥12/15 at the same 80% pass rate. New env-gated `tests/test_rag/test_retrieval_real.py` (`RAG_RUN_MODEL_TESTS=1`) pins the rank-9 chunk in `top_k=DEFAULT_TOP_K` hits via the real `all-MiniLM-L6-v2` retriever — catches the regression without burning an API call (offline suite stubs the embedder so cannot). RED→GREEN proven out-of-band (old top_k=5 misses, new top_k=10 catches). Full offline suite **524 passed / 3 skipped / 1 xfailed / 97%**. Env-gated retrieval: 2 passed in 14.14s. **Live `RAG_RUN_LLM_EVAL=1` ≥12/15 eval PASSED in 75.06s** on `gemini-3.5-flash`. ANS-24 test updated to import `DEFAULT_TOP_K` (bidirectional value + signature pin, self-updating on future bumps). BUG-014 logged + RESOLVED. Branch: feature/rag-retrieval-short-queries. Tier: Medium (full TDD loop incl. two Decision Gates after the first one was contradicted by the probe).
- 2026-06-21 — Phase 4 slice 2 — **headless screenshot capture + demo gif shipped.** New `scripts/capture_screenshots.py` (400 LOC, Playwright + Pillow) + `scripts/_capture_app.py` (32 LOC Streamlit shim that monkeypatches `chat.answer_question` to a canned grounded `Answer` over `failure-modes/tombstoning.md#3` — no live Gemini key needed for the Co-Pilot capture). Owner-approved approval-gated edits: `pyproject.toml` (×2: `playwright>=1.49` in dev group + `[tool.pytest.ini_options].pythonpath = [".", "src"]` so `from scripts import` resolves in tests); `README.md` (`![Demo walkthrough](docs/img/demo.gif)` above the hero strip). New `tests/test_scripts/` package: 38 unit tests for pure helpers + 4 subprocess shim tests = 42 green; 5 env-gated end-to-end correctly skipped. `ui_db_path` fixture lifted from `tests/test_ui/conftest.py` → `tests/conftest.py` per MD-3 (DRY; session-scope unchanged; both test packages resolve by name). Declared parallel edit `tests/test_ui/test_chat_smoke.py:24` `#0`→`#3` (B-1 ripple — keep fixture aligned with new `CANNED_CITATION_ID`). **Suite: 566 passed / 5 skipped / 1 xfailed / 97% coverage on `src/` denominator** (baseline 524/3/1 → +42 unit / +2 env-gated). Zero `src/flying_probe_copilot/**` edits. Zero `.claude/**` edits. Full 12-step Medium loop with parent-driven Steps 8+9 (Triple Check) since tests + live capture had already verified the key claims. Step 5 red-team caught **5 BLOCKERs** (B-1 citation chunk `#0` was the title section not "Likely causes" → `#3`; B-2 `st.expander` defaults collapsed → Playwright click; B-3 pytest needs `pythonpath = [".","src"]` to import `scripts/`; B-4 shim monkeypatch survival → defensive `assert _chat.answer_question is build_canned_answer` + subprocess test; B-5 sidebar nav name won't match Streamlit's emoji-prefixed `"📊 Overview"` → regex match scoped to `[data-testid='stSidebarNav']`) + 12 WARNINGs + 6 MISSING DECISIONs — all closed in Plan-Rev1 before Execute. Live-capture iteration surfaced 2 issues red-team missed: (1) Citations expander is `<details><summary>` not `<button>` (selector fix `get_by_role("button") → get_by_text`); (2) Overview's twin-column Plotly charts in `st.columns` need 4 s initial settle (single-chart pages settled fine at 2 s). 6 portfolio-grade JPGs (62-145 KB; KPIs + real charts + sidebar filters; Co-Pilot shows canned answer + **expanded** Citations expander pinning `failure-modes/tombstoning.md#3` = exact BUG-014 narrative artifact) + 748 KB GIF89a (12 s loop, 2 s/frame, well under 5 MB target). Pre-flight: built `data/db/sample.duckdb` (~3 min, 3×300-panel batches, 18 MB DB). Branch: feature/phase4-slice2-screenshots. Tier: Medium.
- 2026-06-21 — Phase 4 slice 3 — **GitHub Actions CI workflows + ruff config shipped.** Two NEW workflow files: `.github/workflows/ci.yml` (lint job: `ruff check --output-format=github` + `ruff format --check`; tests job: `uv run pytest -q --cov=src/flying_probe_copilot --cov-report=term`; parallel; ubuntu-latest; Python 3.11 via `astral-sh/setup-uv@v8`; `paths-ignore: docs/** + **/*.md + notebooks/** + .claude/**`; top-level `concurrency.cancel-in-progress: true`; `permissions: contents: read`) + `.github/workflows/screenshots.yml` (capture job: triggers on `src/flying_probe_copilot/ui/** | analytics/** | docs/knowledge-base/** | scripts/capture_screenshots.py | scripts/_capture_app.py`; `actions/cache@v4` keyed on `hashFiles(generator/cli.py + generator/** + parser/cli.py + parser/** + build-portfolio-data.sh + uv.lock)` per B-5 non-glob anchors; `restore-keys: sample-duckdb-`; cache-miss runs `bash scripts/build-portfolio-data.sh`; then `playwright install --with-deps chromium`; then `scripts/capture_screenshots.py all --out docs/img-ci/`; uploads via `actions/upload-artifact@v4` `recaptured-dashboard-screenshots` 14-day retention per D2). Owner-approved approval-gated `pyproject.toml` edit: `ruff>=0.6` to `[dependency-groups].dev` (ASCII sort: playwright, pytest-cov, pytest, ruff) + `[tool.ruff]` (`line-length=100`, `target-version="py311"`, `extend-exclude=["data","docs","notebooks",".claude","scripts"]`) + `[tool.ruff.lint].select=["E","F","W","I"]` + `[tool.ruff.lint.per-file-ignores]."tests/**/*.py"=["E501","F401"]` + `[tool.ruff.format]`. Lint preflight surfaced 301 errors + 58 unformatted files → owner ratified D17 = **Option A cleanup pass** as one-time slice-3 exception to read-only `src/**`. Cleanup commit `056459e` applied `ruff check --fix` (189 auto-fixes: I001 + F401 + F811 + E401) + `ruff format` (58 files) across 26 src/ + 42 tests/ files (+1517/-1126). D17.1 hybrid: 4 F841 + 2 E712 per-line `# noqa` + 1 E741 refactor `l` → `shift_val`/`line_val` in test_ingest.py + 11 inline `import duckdb  # noqa: F811` adds in `test_views_smoke.py` (load-bearing for `AppTest.from_function` subprocess execution). New `tests/test_ci/` (3 files): `__init__.py` + `conftest.py` (4 session-scope fixtures with `copy.deepcopy` per W-6) + `test_workflow_yaml.py` (83 `def test_*` + 9 parametrize = 93 collected). **Suite: 659 passing / 5 skipped / 1 xfailed / 97% coverage** (baseline 566/5/1 → +93 ci tests). Zero `.claude/**` edits. Zero `scripts/**` edits. Step 5 red-team caught 7 BLOCKERs (B-1 PyYAML `on:` keyword trap → quote `"on":`; B-2 python-version float coercion callout; B-3 `setup-uv@v3` three majors stale → @v8; B-4 scripts/ missing from extend-exclude → added per D16; B-5 cache-key hashFiles glob collapse → cli.py anchors; B-6 stale uv `--all-groups` → `version: ">=0.7"` floor; B-7 ruff format preflight policy → owner D17/D17.1) + 12 WARNs + 8 MISSING-DECISIONs — all closed in Plan-Rev1 before Execute. Branch: feature/phase4-slice3-ci-workflows (renamed from worktree `claude/sweet-jones-7291db` at Step 10). Tier: Medium (full 12-step loop). **Next: Phase 4 slice 4 — portfolio promotion (final guardrails audit + repo public flip + docs/DEMO.md + blog post + LinkedIn post + resume bullet + branch-protection rules).**
